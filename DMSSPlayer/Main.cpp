// DMSSPlayer.cpp : 定义控制台应用程序的入口点。
//
#include "SDLApplication.h"
#include "Unpacker.h"
#include "AVPlayer.h"
using namespace dmss::ffmpeg;
int main(int argc, char* argv[])
{
	/*
	AVPlayer player;
	Unpacker unpacker(&player);
	player.Open("K:/JavaStudy/test.wmv", 0);
	//unpacker.Init("K:/JavaStudy/test.wmv");
	//*/
	//fprintf(stdout, decoder.DecodeInfo().Duration().c_str());
	//fprintf(stdout, "type = %d\n", decoder.DecodeInfo().mediaType);
	//*
	SDLApplication app;
	app.Execute();
	//*/
//	getchar();
	return 0;
}

